import {Loader2} from "lucide-react";

export const LoadingSpinner = () => (
    <div className="flex items-center justify-center g-[300px">
        <Loader2 className="h-8 w-8 animate-spin text-pink-600" />
    </div>
)